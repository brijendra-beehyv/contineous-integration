name: Deploy metrics app

on:
  push:
    branches: [ "main", "qa", "dev" ]
    tags: [ "v*.*.*" ]
    paths:
      - metrics_app/**
      - .github/workflows/1-metrics-app.yaml
      - .github/workflows/0-build-n-push.yaml

jobs:
  call-docker-build:
    uses: ./.github/workflows/0-build-n-push.yaml
    with:
      image-name: tmp-metrics-app
      dockerfile-dir: metrics_app/
    secrets: inherit
  
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: gg
        run: kubectl --version
